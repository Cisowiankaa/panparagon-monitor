(()=>{
  let lastStore='',fullRowsCache=[];
  const selectedYear=()=>document.getElementById('storeYear')?.value||'';
  const storesTable=()=>document.getElementById('storesTable');
  const storeName=r=>{try{return (r[storeCol]||'Nieznany sklep').trim()||'Nieznany sklep'}catch{return'Nieznany sklep'}};
  const rememberFullRows=src=>{if(Array.isArray(src)&&src.length>=fullRowsCache.length)fullRowsCache=src.slice()};
  const fullRows=()=>fullRowsCache.length?fullRowsCache:(Array.isArray(rows)?rows:[]);
  const rowsForYear=(all,year)=>all.filter(r=>{try{const d=rowDate(r);return d&&String(d.getFullYear())===String(year)}catch{return false}});
  const baseStoreName=()=>{const title=document.getElementById('storeDetailTitle');return (title?.textContent||'').replace(/\s—\s\d{4}$/,'').trim()};
  const availableYears=()=>{const name=lastStore||baseStoreName(),set=new Set();fullRows().forEach(r=>{try{if(name&&storeName(r)!==name)return;const y=rowDate(r)?.getFullYear();if(y)set.add(y)}catch{}});return [...set].sort((a,b)=>b-a)};
  const ensureDetailSelector=()=>{
    const detail=document.getElementById('storeDetail'),title=document.getElementById('storeDetailTitle');if(!detail||!title)return null;
    let box=document.getElementById('storeDetailYearBox');
    if(!box){box=document.createElement('div');box.id='storeDetailYearBox';box.className='actions';box.style.marginTop='14px';box.innerHTML='<label class="small" for="storeDetailYear" style="align-self:center">Rok:</label><select id="storeDetailYear"><option value="">Wszystkie lata</option></select>';const sub=title.parentElement?.querySelector('.sub');(sub||title).insertAdjacentElement('afterend',box);document.getElementById('storeDetailYear').addEventListener('change',e=>switchDetailYear(e.target.value));}
    const sel=document.getElementById('storeDetailYear'),current=sel.value;sel.innerHTML='<option value="">Wszystkie lata</option>'+availableYears().map(y=>`<option value="${y}">${y}</option>`).join('');if([...sel.options].some(o=>o.value===current))sel.value=current;return sel;
  };
  const syncDetailSelector=year=>{const sel=ensureDetailSelector();if(sel&&[...sel.options].some(o=>o.value===String(year)))sel.value=String(year);else if(sel&&!year)sel.value=''};
  const markDetailYear=year=>{const title=document.getElementById('storeDetailTitle'),sub=title?.parentElement?.querySelector('.sub');if(title){const base=title.textContent.replace(/\s—\s\d{4}$/,'');title.textContent=year?base+' — '+year:base}if(sub)sub.textContent=year?`Widok ograniczony do roku ${year}. Kliknij miesiąc, aby zobaczyć paragony z tego okresu.`:'Kliknij miesiąc, aby zobaczyć wszystkie paragony z tego okresu.';syncDetailSelector(year);wireYearRows();};
  const switchDetailYear=year=>{const name=lastStore||baseStoreName();if(!name)return;lastStore=name;const all=fullRows(),detail=year?rowsForYear(all,year):all,api=window.PanParagonStoreDetails;if(api&&typeof api.openStore==='function'){api.openStore(name,detail,all);markDetailYear(year)}const mainYear=document.getElementById('storeYear');if(mainYear)mainYear.value=year;};
  const wireYearRows=()=>{const box=document.getElementById('storeYearTable');if(!box)return;box.querySelectorAll('table tr').forEach((tr,i)=>{if(i===0||tr.dataset.yearWired)return;const first=tr.querySelector('td'),year=(first?.textContent||'').trim();if(!/^\d{4}$/.test(year))return;tr.dataset.yearWired='1';tr.style.cursor='pointer';tr.title=`Pokaż tylko rok ${year}`;if(first)first.innerHTML=`<button type="button" style="padding:0;border:0;background:transparent;color:var(--a);font-weight:800;cursor:pointer">${year}</button>`;tr.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();switchDetailYear(year)});});};
  const onCapture=e=>{const table=storesTable();if(!table||!table.contains(e.target))return;const tr=e.target.closest('tr');if(!tr||tr.rowIndex===0)return;const name=(tr.querySelectorAll('td')[1]?.textContent||'').trim();if(name)lastStore=name;if(Array.isArray(rows))rememberFullRows(rows);const year=selectedYear();if(!year)return;const all=fullRows(),api=window.PanParagonStoreDetails;setTimeout(()=>{if(api&&typeof api.openStore==='function')api.openStore(name,rowsForYear(all,year),all);markDetailYear(year)},0);};
  const install=()=>{if(Array.isArray(rows))rememberFullRows(rows);const table=storesTable();if(table)table.addEventListener('click',onCapture,true);const detail=document.getElementById('storeDetail');if(detail)new MutationObserver(()=>{if(detail.classList.contains('on')){ensureDetailSelector();wireYearRows()}}).observe(detail,{attributes:true,attributeFilter:['class']});const yearTable=document.getElementById('storeYearTable');if(yearTable)new MutationObserver(wireYearRows).observe(yearTable,{childList:true,subtree:true});ensureDetailSelector();wireYearRows();};
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();
})();